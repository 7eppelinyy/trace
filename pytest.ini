[pytest]
# 默认排除 live 网络测试（默认单元测试不得依赖公网稳定性）
addopts = -m "not live"
# 关键：把仓库根目录加进 sys.path，保证 CI 上裸跑 `pytest`（非 `python -m pytest`）
# 也能导入 trace 包。缺这行会报 "No module named 'trace.config'; 'trace' is not a package"。
pythonpath = .
markers =
    live: 实时网络测试（需要公网与真实来源可达），默认跳过
